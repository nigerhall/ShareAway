<%= form_for(current_user.relationships.build(watching_id: @profile.id)) do |f| %>
  <div><%= f.hidden_field :watching_id %></div>
  <%= f.submit "Connect", class: "btn btn-large btn-primary" %>
  
<% end %>

 